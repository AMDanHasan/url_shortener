<h1>New URL to be Shortened</h1>

<%= form_with scope: :website, url: websites_path, local: true do |form| %>
  <p>
    <%= form.label :url %><br>
    <%= form.text_field :url %>
  </p>

  <p>
    <%= form.submit %>
  </p>

  <p><%= link_to 'Back to Index', :root %></p>
<% end %>
